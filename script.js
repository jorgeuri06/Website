// Get the button element
const joinBtn = document.querySelector('.join-btn');

// Add a click event listener to the button
joinBtn.addEventListener('click', () => {
  // Navigate to the page.html file
  window.location.href = 'page.html';
});